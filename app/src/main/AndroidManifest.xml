<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.harrak.devoir3">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuideUniversitaire">
        <activity android:name=".SupprimerActivity2"
            android:label="Supprimer une université"
            android:parentActivityName=".SupprimerActivity" ></activity>
        <activity android:name=".UpdateActivity"
            android:label="Modifier une université"
            android:parentActivityName=".ListerActivity" />
        <activity android:name=".ListerActivity"
            android:label="Liste des universités"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".SupprimerActivity"
            android:label="Supprimer à partir la liste"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".AjouterActivity"
                android:label="Ajouter une université"
                android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>